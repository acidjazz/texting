.wrapper

  each message, index in messages
    .message(class=message.which)
      if message.picture
        .picture
          img(src=message.picture)
      else
        .picture.icon-user
      .copy #{message.copy}
      .date 2 hours ago
