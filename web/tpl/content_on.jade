.contacts.loading
  .resizer(data-for='.contacts').hint--right(data-hint='click to drag or double click to minimize/restore')
    .ghost
    .handle
      .dot  &middot
      .dot  &middot
      .dot  &middot
  .body
    include _loader
    .clear
.boxes .boxes (user logged in as #{user.name})
.clear
